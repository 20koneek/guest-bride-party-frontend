# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

type Contest {
    conditions: [ContestCondition!]!
    id: ID!
    name: String!
}

type ContestCondition {
    id: ID!
    name: String!
}

type Guest {
    card: GuestCard
    id: ID!
    name: String!
}

type GuestCard {
    id: ID!
    status: CardStatus!
}

type Mutation {
    addCard: String!
    createGuest(input: GuestInput!): Guest!
    createPayment(input: PaymentInput!): String!
    skipCard: Guest!
    updateCard(id: String!, status: CardStatus!): Guest!
    updatePaymentStatus(id: String!, status: PaymentStatus!): Payment!
}

type Payment {
    amount: Int!
    contestCondition: ContestCondition!
    id: ID!
    status: PaymentStatus!
}

type Query {
    currentContest(id: String!): Contest!
    currentContests: [Contest!]!
    currentGuest: Guest!
    currentPayments: [Payment!]!
    currentWedding: Wedding!
}

type Wedding {
    contests: [Contest!]!
    id: ID!
    name: String!
}

enum CardStatus {
    Confirmed
    Failed
    Init
    Skipped
}

enum PaymentStatus {
    Failed
    Finished
    Init
    Run
}

input GuestInput {
    name: String!
    weddingId: String!
}

input PaymentInput {
    amount: Float!
    conditionId: String!
}
